interface:
  display_name: "SkillSpector Review"
  short_description: "SkillSpector, OWASP, and semantic skill review"
  default_prompt: "Use $skillspector-review to scan this repository with SkillSpector and OWASP checks, then ask whether I want local Codex or Claude Fable 5 via OpenRouter for semantic review."
policy:
  allow_implicit_invocation: true
